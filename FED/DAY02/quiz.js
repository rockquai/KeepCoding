'use strict';

// 1. 2개의 숫자를 인자로 받아 그 합을 반환하는 함수를 만들어라.
function sumCalcu(num1, num2) {
	return num1 + num2;
}

// 2. 2개의 숫자를 인자로 받아 더 큰 숫자를 반환하는 함수를 만들어라.
function compareNum(num1, num2) {
	return (num1 > num2) ? num1 : num2;
}

// 3. 숫자를 하나 받아 음수일 경우 false를, 양수일 경우 true를 반환하는 함수를 만들어라(0은 false 반환)
// 비교연산자 자체가 불린값으로 반환, 삼항연산자 조건식이 필요없다.
function isNum(n) {
	return num > 0;
}